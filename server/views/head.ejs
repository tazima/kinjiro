<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" type="text/css" href="<%= assetFingerprint("/build.css") %>" />
  </head>
  <% if (bodyId) { %>
  <body id="<%- bodyId %>">
  <% } else { %>
  <body>
  <% } %>
